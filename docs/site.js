document.querySelectorAll('[data-copy]').forEach(button=>button.addEventListener('click',async()=>{const text=document.getElementById(button.dataset.copy).textContent;const status=document.getElementById(button.dataset.copyStatus || 'copy-status');try{await navigator.clipboard.writeText(text);button.textContent='Copied';if(status)status.textContent='Copied. Paste this request into your assistant.';setTimeout(()=>button.textContent='Copy',2200)}catch{if(status)status.textContent='Copy is unavailable in this browser. Select and copy the request above.'}}));
const samples={first:{input:'Change the parser and delimiter settings together, then ship after one successful file.',heading:'Use one controlled experiment at a time.',note:'Uses three procedures from the saved lesson.'},second:{input:'I reproduced the bug and changed one variable. Ship after one passing example.',heading:'Add a boundary check before shipping.',note:'Reuses the same three saved procedures. No source download or new extraction.'}};
document.querySelectorAll('[data-example]').forEach(button=>button.addEventListener('click',()=>{document.querySelectorAll('[data-example]').forEach(b=>{b.classList.toggle('selected',b===button);b.setAttribute('aria-pressed',String(b===button))});const sample=samples[button.dataset.example];if(sample&&document.getElementById('sample-input')){document.getElementById('sample-input').textContent=sample.input;document.getElementById('sample-heading').textContent=sample.heading;document.getElementById('reuse-note').textContent=sample.note}}));
