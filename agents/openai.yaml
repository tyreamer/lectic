interface:
  display_name: "Lectic"
  short_description: "Discover what your content can become, then build it"
  default_prompt: "Use $expertise-compiler to save these sources, discover their strongest supported opportunities, and recommend what to build first."
policy:
  allow_implicit_invocation: true
